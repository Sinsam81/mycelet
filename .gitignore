node_modules
.next
out
.env
.env.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store
coverage
tsconfig.tsbuildinfo

# Playwright
/test-results
/playwright-report
/playwright/.cache
/blob-report
e2e/.auth

# Supabase CLI scratch (contains the project ref) — never commit
supabase/.temp/

# iCloud sync duplicates ("fil 2.ext") — never commit these
* 2.*
* 3.*

# Unrelated local folder living inside the project dir
Rørlegger.no/
